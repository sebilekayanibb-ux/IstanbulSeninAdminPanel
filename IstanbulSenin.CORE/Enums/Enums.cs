namespace IstanbulSenin.CORE.Enums
{
    // JSON içindeki "permissions"
    public enum PermissionType { location, camera, microphone, file }

    // JSON içindeki "plugins"
    public enum PluginType { ibbwifi }
}